/* @flow */

export const pageStyle = `
    html, body {
        padding: 0;
        margin: 0;
        width: 100%;
        overflow: hidden;
        text-align: center;
    }

    * {
        touch-callout: none;
        user-select: none;
        cursor: default;
    }
`;
